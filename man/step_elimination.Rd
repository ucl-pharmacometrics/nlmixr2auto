% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sf.operator.R
\name{step_elimination}
\alias{step_elimination}
\title{Screen elimination type (Michaelis–Menten on/off)}
\usage{
step_elimination(
  dat,
  state,
  search.space,
  param_table,
  penalty.control = NULL,
  precomputed_results_file = NULL,
  filename = NULL,
  ...
)
}
\arguments{
\item{dat}{Dataset passed to \code{mod.run()}.}

\item{state}{List-like with optional fields:
\itemize{
\item \code{best_code}: named integer vector (starting code). If NULL, \code{base_model()} is used.
\item \code{modi}: forwarded to \code{mod.run()} (default \code{1} if NULL).
}}

\item{search.space}{Character scalar: either \code{"ivbase"} or \code{"oralbase"}.}

\item{param_table}{Parameter table passed to \code{mod.run()}.}

\item{...}{Additional arguments forwarded to \code{mod.run()}. May include
\code{custom_base} used by \code{base_model()}.}
}
\value{
A list with:
\itemize{
\item \code{results_table}: \code{data.frame} with columns Step, Model name, Model code, Fitness
\item \code{best_code}: named integer vector of the best candidate’s model code
\item \code{best_row}: one-row \code{data.frame} (the best candidate)
}
}
\description{
This step compares a standard linear elimination model (\code{mm = 0})
versus a Michaelis–Menten elimination model (\code{mm = 1}) while keeping
all other positions of the current model code unchanged.
}
\details{
If \code{mm = 0}, any IIV term for Km (\code{eta.km}) will be automatically set to 0.
}
\examples{
\dontrun{
# Example dataset and parameter table would normally come from your workflow
dat <- your_data_frame
param_table <- your_param_table

# Initialize state with a base model
state <- list(best_code = base_model("ivbase"), modi = 1)

# Run elimination type screening
res_mm <- step_elimination(
  dat = dat,
  state = state,
  search.space = "ivbase",
  param_table = param_table
)

# View results
res_mm$results_table
res_mm$best_code
}

}
